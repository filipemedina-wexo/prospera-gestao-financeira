
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const DashboardPessoas = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Dashboard de Pessoas</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Visão geral do módulo de gestão de pessoas. Em desenvolvimento.</p>
      </CardContent>
    </Card>
  );
};

export default DashboardPessoas;
