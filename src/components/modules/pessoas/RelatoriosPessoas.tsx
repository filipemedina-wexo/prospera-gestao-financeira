
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const RelatoriosPessoas = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Relatórios de RH</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Relatórios sobre colaboradores, custos e métricas de RH. Em desenvolvimento.</p>
      </CardContent>
    </Card>
  );
};

export default RelatoriosPessoas;
